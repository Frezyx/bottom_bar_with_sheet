import 'package:bottom_bar_with_sheet/src/theme/bottom_bar_with_sheet_theme.dart';
import 'package:bottom_bar_with_sheet/src/theme/main_action_button_theme.dart';
import 'package:flutter/material.dart';

const defaultBarTheme = BottomBarTheme();
const defaultMainActionButtonTheme = MainActionButtonTheme();
const defaultMargin = EdgeInsets.all(5);
const defaultCurve = Curves.linear;
const defaultDuration = Duration(milliseconds: 500);
